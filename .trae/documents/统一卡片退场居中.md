## 现状
当前卡片切换动画在 [VibeCodingPage.tsx:L766-L782](file:///Users/bytedance/Desktop/tryout/src/vibe-coding/VibeCodingPage.tsx#L766-L782)，`exit` 的 `y: -36` 会让退场瞬间上移，若容器尺寸在变化或最后一张比例更竖，会看起来“脱离中心”。

## 目标
- 最后一张退场仍然保持在中心区域缩放，不出现“偏离中心”的观感。
- 统一所有卡片的 scale in/out 与位置逻辑。

## 方案
1. 对退场动画改为 **以中心缩放为主**，y 位移减少：
   - `exit: { opacity: 0, scale: 0.88, y: -12 }`
2. 进场位移保持从下方进入，但略微减少幅度，确保中心感一致：
   - `initial: { opacity: 0, scale: 1.22, y: 36 }`
3. 保持 animate `y: 0`，确保中心。
4. 其他逻辑不改。

确认后我就调整这一段参数。