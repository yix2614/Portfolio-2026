## 结论
当前 UI 失真主要来自自研 runtime 与生成代码不完全兼容；同时 ProjectCard/Divider 仍是转导出而非真正拆分。需要先恢复 1:1 UI，再做物理拆分。

## 计划
1. 恢复 1:1 UI
   - 临时切回生成代码依赖的完整渲染路径，确保 ProjectMenu 视觉与交互完全一致
   - 校验所有 hover/variant/视频/图片/链接与原始一致
2. 物理拆分组件
   - 从 ProjectMenu.tsx 中提取 ProjectCard 与 ProjectDivider 的真实实现到各自文件
   - ProjectMenu.tsx 仅组合与导入，不再内嵌实现
3. 稳定自研化
   - 在 UI 一致的基线下，逐步用自研实现替换 unframer 行为
   - 每次替换后保持 UI 1:1，对比验证
4. 验证
   - 本地预览对比与控制台无错误
   - 外链保持不变

确认后我就开始按这个顺序执行。