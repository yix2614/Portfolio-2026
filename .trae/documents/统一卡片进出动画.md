## 现状
- 卡片切换动画在 [VibeCodingPage.tsx:L764-L782](file:///Users/bytedance/Desktop/tryout/src/vibe-coding/VibeCodingPage.tsx#L764-L782) 里已经有 `scale` + `y`，但只在 `currentIndex !== 0` 时生效，且首张/hero 特例逻辑较多。

## 目标
- 只改 2～4 张卡（index 1~3）的进出动画。
- 入场：从下方、缩放更大一点进入。
- 退场：缩小、向上离开。
- 动画要丝滑、不复杂。
- 其他内容不改。

## 计划
1. 在 `motion.div` 动画配置里引入一个明确的判断（例如 `shouldAnimateCard = !isHeroTransitioning && currentIndex >= 1`），仅对 index 1~3 应用动画。
2. 统一 `initial/animate/exit`：
   - initial：`opacity: 0, scale: 1.18, y: 40`（更明显的大一点）
   - animate：`opacity: 1, scale: 1, y: 0`
   - exit：`opacity: 0, scale: 0.9, y: -32`
3. 统一 transition：`duration: 0.55`，`ease: [0.22, 1, 0.36, 1]`（平滑但不复杂）。
4. 保持 hero 与 index 0 的特殊逻辑不变（不做切换动画）。

确认后我就改代码。